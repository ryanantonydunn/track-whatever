import { Container } from "@mui/material";
import { Layout } from "../base/Layout";

export const DataCompareList: React.FC = () => {
  return (
    <Layout title="Compare Data" back={false}>
      <Container maxWidth="sm">DataCompareList</Container>
    </Layout>
  );
};
